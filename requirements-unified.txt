# Merged dependencies for HyConEx + access_explainer (install in one venv).
# Install HyConEx first: pip install -r HyConEx/requirements.txt
# Then: pip install -r requirements-unified.txt

torch>=2.0
numpy>=1.22
scikit-learn>=1.2
pandas>=2.0
# HyConEx also pulls nflows, tqdm, omegaconf, wandb, etc. — see HyConEx/requirements.txt
